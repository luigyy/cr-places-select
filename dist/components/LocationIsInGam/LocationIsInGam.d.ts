type PlaceLocationType = {
    province: string;
    municipality: string;
};
/**
 * check if location is in Gam
 */
export default function LocationIsInGam(location: PlaceLocationType): boolean;
export {};
